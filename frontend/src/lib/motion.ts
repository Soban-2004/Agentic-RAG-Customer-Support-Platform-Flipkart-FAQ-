// The one easing curve reused across every animated component (see
// index.css's --ease-ui and the "motion consistency" design rule).
export const EASE_UI: [number, number, number, number] = [0, 0, 0.2, 1]
